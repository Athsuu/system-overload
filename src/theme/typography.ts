/** Typographie UI — Dark Hex Terminal (latin FR + EN). */
export const FONT_DISPLAY = "'Rajdhani', system-ui, sans-serif";

/** Décalage global des tailles de texte UI (px). */
export const UI_FONT_BUMP_PX = 4;