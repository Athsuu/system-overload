/**
 * Player-facing narrative — locale-aware.
 * Use `useGameStrings()` in React components, `getGameStrings()` elsewhere.
 */
export { getGameStrings, type GameStrings } from '../i18n';
export { useGameStrings } from '../i18n/useGameStrings';
