import type { CoreProtocolId } from '../store/prestigeTypes';

/** Ordre d'affichage du Monolithe, de la base (fondations) au sommet (boucle méta). */
export const SEED_PROTOCOL_IDS: CoreProtocolId[] = [
  'bootReinforcement',
  'thermalBaseline',
  'residualMemory',
  'extractionProtocol',
  'seedResonance',
];
